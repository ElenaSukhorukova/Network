<h1><%= t('page.edit', model: i18n_model_name(@post)) %></h1>

<%= render partial: 'form', locals: { post: @post, method: :put, 
  url: post_path(@post), submit: t('submit.edit', model: i18n_model_name(@post).downcase), place: @post.place } %>