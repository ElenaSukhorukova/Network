<section>
  <% provide :page_title, t('page.edit', model: i18n_model_name(@group).downcase) %>
  <% currently_at %>
  <h1 class='md-4'><%= t('page.edit', model: i18n_model_name(@group).downcase) %></h1>

  <%= render partial: 'form',  locals: { model: @group, method: :put, 
    submit: t('submit.edit', model: i18n_model_name(@group).downcase) } %>
</section>