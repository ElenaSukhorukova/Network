<%= turbo_frame_tag dom_id(@post) do %>
  <h3 class='mt-4 mb-2'><%= t('page.new', model: i18n_model_name(@post).downcase) %></h3>

  <%= render partial: 'shared/go_back_button', locals: { path: account_path(@account) } %>
  <%= render partial: 'posts/form',  locals: { model: [@account, @post], method: :post } %>
<% end %>
