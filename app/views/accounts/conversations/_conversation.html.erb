<% active_class = (@conversation == conversation) ? "btn btn-dark my-4" : "btn btn-outline-dark my-4" %>
<%= link_to "#{find_interlocutor(@account.id, conversation).user_name}", 
    account_conversation_path(@account, conversation), class: active_class, role: 'button' %>
