<%= turbo_frame_tag dom_id(@message) do %>
  <%= render partial: "accounts/messages/form", locals: { model: @message, 
    method: :put, submit: t('submit.edit', model: i18n_model_name(@message).downcase), 
    path: account_conversation_path(current_user.account, @message.conversation) } %>
<% end %>