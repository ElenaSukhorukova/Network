<%= simple_form_for comment, method: method, url: url do |f| %>
	<%= f.input :body %><br />
	<%= f.button :submit, submit, class: "btn btn-outline-dark" %>
	<%= render partial: 'shared/cancel_button', locals: { path: path } if f.object.persisted? %>	
<% end %>
