<% provide :page_title, i18n_model_name(Account.new, count: 2) %>
<% currently_at %>

<article class='article'>
  <% if @accounts.try(:any?) %>
    <h2 class='mt-4 mb-2'><%= i18n_model_name(Account.new, count: 2) %></h2>
    <%= render @accounts %>
    <div class='my-2'>
      <%== pagination(@pagy) %>
    </div>
  <% end %>
</artcile>
